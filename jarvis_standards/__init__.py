"""jarvis_standards — runtime checks and adapters for cross-repo standards.

Docs, ADRs, and canonical port *interfaces* live in ``ports/`` and ``docs/``.
This package holds the small amount of runnable code the standards repo owns
directly: deterministic checks (e.g. the model-fit check) and their default
pure-Python adapters.
"""
